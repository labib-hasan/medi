import DirectorMessagePage from "../../components/DirectorMessagePage";

export default function DirectorOnePage() {
  return <DirectorMessagePage director={{ slug: "director-1", title: "Message from Director 1", name: "Director 1", position: "Director" }} />;
}
