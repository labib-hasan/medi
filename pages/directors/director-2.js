import DirectorMessagePage from "../../components/DirectorMessagePage";

export default function DirectorTwoPage() {
  return <DirectorMessagePage director={{ slug: "director-2", title: "Message from Director 2", name: "Director 2", position: "Director" }} />;
}
